import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:animalcargo/app/modules/home/select_route/select_route_page.dart';

main() {
  testWidgets('SelectRoutePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SelectRoutePage(title: 'SelectRoute')));
    //  final titleFinder = find.text('SelectRoute');
    //  expect(titleFinder, findsOneWidget);
  });
}
