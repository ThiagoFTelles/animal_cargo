import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:animalcargo/app/modules/home/components/saved_place/saved_place_controller.dart';
import 'package:animalcargo/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SavedPlaceController savedplace;
  //
  setUp(() {
    //     savedplace = HomeModule.to.get<SavedPlaceController>();
  });

  group('SavedPlaceController Test', () {
    //   test("First Test", () {
    //     expect(savedplace, isInstanceOf<SavedPlaceController>());
    //   });

    //   test("Set Value", () {
    //     expect(savedplace.value, equals(0));
    //     savedplace.increment();
    //     expect(savedplace.value, equals(1));
    //   });
  });
}
