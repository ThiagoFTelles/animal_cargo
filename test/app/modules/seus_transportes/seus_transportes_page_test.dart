import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:animalcargo/app/modules/seus_transportes/seus_transportes_page.dart';

main() {
  testWidgets('SeusTransportesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SeusTransportesPage(title: 'SeusTransportes')));
    //  final titleFinder = find.text('SeusTransportes');
    //  expect(titleFinder, findsOneWidget);
  });
}
