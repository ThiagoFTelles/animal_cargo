import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:animalcargo/app/modules/pass_recovery/pass_recovery_controller.dart';
import 'package:animalcargo/app/modules/pass_recovery/pass_recovery_module.dart';

void main() {
  initModule(PassRecoveryModule());
  // PassRecoveryController passrecovery;
  //
  setUp(() {
    //     passrecovery = PassRecoveryModule.to.get<PassRecoveryController>();
  });

  group('PassRecoveryController Test', () {
    //   test("First Test", () {
    //     expect(passrecovery, isInstanceOf<PassRecoveryController>());
    //   });

    //   test("Set Value", () {
    //     expect(passrecovery.value, equals(0));
    //     passrecovery.increment();
    //     expect(passrecovery.value, equals(1));
    //   });
  });
}
