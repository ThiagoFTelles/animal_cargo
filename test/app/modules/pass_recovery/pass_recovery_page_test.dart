import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:animalcargo/app/modules/pass_recovery/pass_recovery_page.dart';

main() {
  testWidgets('PassRecoveryPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PassRecoveryPage(title: 'PassRecovery')));
    //  final titleFinder = find.text('PassRecovery');
    //  expect(titleFinder, findsOneWidget);
  });
}
