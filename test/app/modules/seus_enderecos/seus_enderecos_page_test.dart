import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:animalcargo/app/modules/seus_enderecos/seus_enderecos_page.dart';

main() {
  testWidgets('SeusEnderecosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SeusEnderecosPage(title: 'SeusEnderecos')));
    //  final titleFinder = find.text('SeusEnderecos');
    //  expect(titleFinder, findsOneWidget);
  });
}
